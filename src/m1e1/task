В этом задании нам предстоит реализовать метод, который находит
первый интересующий нас элемент из коллекции любого типа, который
унаследован от Human. Сигнатура метода выглядит следующим образом:

public T findFirst(final List<T> humans, final String name, final Sex sex);

В коллекции humans метод должен найти первый элемент, который имеет имя,
которое начинается с name и пол sex, и вернуть результат. При этом метод
должен корректно работать с любым типом, который унаследован от Human, например: Man/Woman.